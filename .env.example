# --- Required ---
# Discord bot token (Developer Portal → Bot → Reset Token)
DISCORD_TOKEN=your-bot-token-here
# DeepSeek API key (platform.deepseek.com/api_keys)
DEEPSEEK_API_KEY=sk-your-deepseek-api-key-here

# --- Discord ---
# Prefix for text commands (!ask, !clear, ...)
BOT_PREFIX=!
# Status line shown under the bot's name
BOT_STATUS=DeepSeek — /ask or @mention me
# Comma-separated channel/thread IDs where the bot answers every message (no mention needed)
ALLOWED_CHANNELS=

# --- DeepSeek API ---
DEEPSEEK_BASE_URL=https://api.deepseek.com
# deepseek-chat (default) or deepseek-reasoner
DEEPSEEK_MODEL=deepseek-chat
# Seconds to wait for the API before timing out
REQUEST_TIMEOUT_SECONDS=60
# OpenAI SDK auto-retries transient failures up to this many times
MAX_RETRIES=2

# --- Conversation behaviour ---
# Rolling history kept per channel/thread
MAX_CONTEXT_MESSAGES=20
# Bot replies kept in the rolling context (min 1). Fewer = the bot sees less of
# its own past replies, so it repeats itself / drags on old topics much less.
MAX_ASSISTANT_MESSAGES=4
# Max tokens per reply
MAX_TOKENS=2048
# Creativity (0.0 strict → 1.0 creative)
TEMPERATURE=0.7
# Live-edit streaming replies; set false for typing-indicator + buffered replies
STREAM_RESPONSES=true
# Override the AI's system prompt (optional). Leave commented out for the built-in persona.
# SYSTEM_PROMPT=

# --- Rate limiting ---
# Max prompts a single user may send per sliding window (0 disables)
ASK_COOLDOWN_RATE=5
ASK_COOLDOWN_PERIOD_SECONDS=60

# --- Agent tools (web search / page fetch / GIFs) ---
# Let the model call tools via function calling
ENABLE_TOOLS=true
# Safety cap on tool calls per request
MAX_TOOL_ITERATIONS=3
# Page content fed back to the model per fetch (token cost!)
PAGE_FETCH_MAX_CHARS=6000
# GIF provider: klipy (default, what Discord uses) | tenor | giphy
GIF_PROVIDER=klipy
# GIF API key — free: Klipy partner.klipy.com · Giphy developers.giphy.com
# (TENOR_API_KEY is used as a fallback if GIF_API_KEY is not set)
GIF_API_KEY=

# --- Random moods ---
# Chance (0..1) of the bot briefly switching to a random personality mood
# (tweak / esquizofrenia / lock-in) per reply. 0 disables.
MOOD_CHANCE=0.2
